% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_SNA4tutti.R
\name{check_and_update_github}
\alias{check_and_update_github}
\title{Check a GitHub-hosted package and optionally offer an update}
\usage{
check_and_update_github(
  pkg,
  ref = "main",
  description_lines = NULL,
  installed_version = NULL,
  ask = interactive()
)
}
\arguments{
\item{pkg}{Character scalar in `"owner/repo"` format.}

\item{ref}{Character scalar indicating the Git ref to inspect. Defaults to
`"main"`.}

\item{description_lines}{Optional character vector containing the remote
`DESCRIPTION` file. When supplied, no network request is made.}

\item{installed_version}{Optional installed version string. When supplied, the
local package library is not queried.}

\item{ask}{Logical scalar indicating whether the user may be prompted to
install or update the package.}
}
\value{
An invisible logical value. `TRUE` means the installed package is at
  least as recent as the GitHub version, `FALSE` means GitHub reports a newer
  version, and `NA` means that the package is not installed or the remote
  version could not be determined.
}
\description{
Retrieves version information for a package hosted on GitHub and compares it
with the version currently installed locally.
}
\details{
This helper keeps the user-facing logic in one place and is written to be
easy to test. For that reason, both the installed version and the remote
`DESCRIPTION` contents can be injected directly, so tests do not need network
access.
}
\keyword{internal}
